# Pokédex - HTML/JS pur

Trois fichiers séparés :

- `index.html` → structure minimale (juste le squelette HTML)
- `style.css` → tout le style
- `script.js` → toute la logique (fetch, DOM, events)

## Lancer

```bash
python3 -m http.server 8080
# → http://localhost:8080
```
